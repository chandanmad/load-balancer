[package]
name = "load-balancer"
version = "0.1.0"
edition = "2024"

[dependencies]
async-trait = "0.1"
env_logger = "0.11.8"
pingora = { version = "0.6.0", features = ["lb", "proxy", "time"] }
pingora-limits = "0.6.0"
